package pkg.Shape;

public sealed class Circle extends Shape permits pkgShape.Ellipse(double test){

          private double radius();
          public double area();
          
          
          protected double setRadius(double num){
              this.radius = num;
          }
          
          protected double getRadius(){
              return this.radius;
          }
          

          if (getRadius() > 0){
              return
          
}
